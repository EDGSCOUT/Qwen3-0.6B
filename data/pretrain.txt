大语言模型的预训练通常使用大规模纯文本语料。训练目标是根据前面的 token 预测下一个 token，因此模型会学习语言结构、常识关联和领域表达方式。

继续预训练通常从已有基座模型出发，在某个领域的文本上继续做因果语言模型训练。它适合让模型吸收领域术语、文档风格和知识分布，但不会直接教模型遵循指令。

监督微调使用的是指令、问题和答案数据。它的目标是让模型在给定用户输入时生成期望回复。预训练和监督微调可以串联使用：先继续预训练，再做监督微调。

